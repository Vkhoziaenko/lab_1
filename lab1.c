#include <stdio.h>
#include <math.h>
int main()
{
	

float x, y, z, a, b, c, s;
scanf("%f,&x");
x=-2.235*pow(10,-2);
y=2.23;
z=15.221;
a=pow(exp,fabs(x-y))*pow(fabs(x-y),x+y);
b=atan(x)+atan(z);
c=pow(pow(x,6)+pow(log,2)y,1\3);
s=a\b+c;
printf("%f", s);

	return 0;
}
